import { GeneralWorkshopResolver } from './general-workshop-resolver';

/*describe('GeneralWorkshopResolver', () => {
  it('should create an instance', () => {
    expect(new GeneralWorkshopResolver()).toBeTruthy();
  });
});*/
